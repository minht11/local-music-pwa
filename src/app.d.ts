declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		interface PageData {
			hidePlayerOverlay?: boolean
			hideBackButton?: boolean
			pageTitle?: string
			title: string
		}
		// interface Platform {}
	}
}
